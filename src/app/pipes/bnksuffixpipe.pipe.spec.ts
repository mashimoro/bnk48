import { BnksuffixpipePipe } from './bnksuffixpipe.pipe';

describe('BnksuffixpipePipe', () => {
  it('create an instance', () => {
    const pipe = new BnksuffixpipePipe();
    expect(pipe).toBeTruthy();
  });
});
